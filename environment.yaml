name: baesystems
channels:
  - defaults
  - conda-forge
  - pytorch
dependencies:
  - python=3.9
  - pip
  - pytorch
  - torchvision
  - torchaudio
  - cudatoolkit=11.3
  - numpy
  - opencv
  - matplotlib
  - pypdf2
  - pip:
      - ultralytics  # YOLOv8 package
      - timm  # Swin Transformer (in the timm library)
      - geojson
      - fpdf
      - kneed
